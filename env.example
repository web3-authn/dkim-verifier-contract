# NEAR account ID where the EmailDkimVerifier contract will be deployed.
CONTRACT_ID=your-contract.testnet
NEAR_NETWORK_ID=testnet

# Keys for the deployer account (used by deploy/upgrade scripts)
DEPLOYER_PUBLIC_KEY=ed25519:
DEPLOYER_PRIVATE_KEY=ed25519:

# Account ID that will sign request_email_verification calls
SIGNER_ID=your-account.testnet

# Set to `outlayer.near` on mainnet, `outlayer.testnet` on testnet
OUTLAYER_CONTRACT_ID=

# Outlayer secrets binding (choose one).
# - Project-based (recommended): e.g. w3a-v1.near/tatchi-xyz-email-recovery
SECRETS_PROJECT_ID=
# - Legacy owner-based: NEAR account ID that owns the secrets (less portable across versions)
SECRETS_OWNER_ID=

# Optional: use an Outlayer Project as the worker source (recommended).
# If set, contract calls use `source: { Project: { project_id } }`.
OUTLAYER_WORKER_PROJECT_ID=
